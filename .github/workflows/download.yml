name: Download new pages from regeringen.se
on:
  workflow_dispatch:
  schedule:
    - cron: "0 1 * * *" # 1AM UTC every day
jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/checkout@v6
        with:
          ref: "data"
          path: "data"
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          version: "latest"
      - run: uv sync --frozen
      - name: Install browser (retry)
        run: |
          for i in {1..3}; do
            uv run camoufox sync
            uv run camoufox set official/stable
            uv run camoufox fetch && exit 0
            echo "camoufox fetch failed (attempt $i). Retrying in $((i*10))s..."
            sleep $((i*10))
          done
          echo "camoufox fetch failed after retries" >&2
          exit 1
      - name: Smoke test stable regeringen.se pages
        run: uv run python -m pytest tests/test_web_parser.py tests/test_parser_smoke_live.py -q
      - name: Fetch the data
        run: uv run fetch.py
      - name: Format changed Markdown
        working-directory: data
        run: |
          {
            git diff --name-only --diff-filter=ACMR -z -- '*.md'
            git ls-files --others --exclude-standard -z -- '*.md'
          } | xargs -0 -r -n 500 ../.venv/bin/rumdl fmt
      - name: Commit and Push
        run: |
          cd ./data
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git add .
          git commit -m "Update the data" || echo "Nothing to push"
          git push
